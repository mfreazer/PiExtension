{
  "name": "pi-context-mesh-extension",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Minimal Context-Mesh Pi extension prototype using summary plus addressable context references.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "devDependencies": {}
}
